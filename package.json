{
  "name": "works",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "frontend"
  ],
  "scripts": {
    "build": "yarn --cwd frontend build && wails build",
    "dev": "wails dev -debounce 1000 2>&1 | grep -v -E 'KnownStructs:|Not found:'",
    "install": "yarn --cwd frontend install && go mod tidy && yarn build",
    "lint": "yarn --cwd frontend lint && golangci-lint run ./...",
    "lint:go": "golangci-lint run ./...",
    "lint:tsx": "yarn --cwd frontend lint",
    "start": "yarn dev",
    "test": "yarn test:go && yarn test:tsx && yarn test:packages",
    "test:go": "go test ./...",
    "test:tsx": "yarn --cwd frontend test",
    "test:packages": "yarn --cwd packages/ui test && yarn --cwd packages/scaffold test",
    "type-check": "yarn --cwd frontend type-check && yarn --cwd packages/ui type-check && yarn --cwd packages/scaffold type-check"
  },
  "dependencies": {}
}
